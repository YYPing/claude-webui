# OpenCode 桌面版功能参考

基于 OpenCode 桌面版数据结构和功能，为 Claude Web UI 提供参考设计。

---

## OpenCode 桌面版核心功能

### 1. 📁 工作区/项目管理
**数据路径**: `/storage/project/`

- 项目目录管理
- 当前工作目录状态
- 项目元数据（名称、路径、最后访问时间）
- 快速切换最近项目

**UI 参考**:
```
┌─────────────────────────────────────┐
│  📁 项目选择器                        │
│  ┌────────────────────────────────┐ │
│  │ 🏠 my-project                 │ │
│  │ 📂 /Users/yyp/projects/webui  │ │
│  └────────────────────────────────┘ │
│  最近项目:                           │
│  • claude-webui                     │
│  • my-app                           │
│  • dashboard                        │
└─────────────────────────────────────┘
```

---

### 2. 💬 会话管理 (Session)
**数据路径**: `/storage/session/`

- **会话列表**: 所有历史会话
- **会话信息**: 
  - 会话 ID
  - 创建时间
  - 最后更新时间
  - 会话标题（可编辑）
  - 关联项目
- **会话操作**:
  - 新建会话
  - 重命名
  - 删除
  - 搜索/筛选

**UI 参考**:
```
┌─────────────────────────────────────┐
│  💬 会话                             │
│  ┌────────────────────────────────┐ │
│  │ ➕ 新会话                       │ │
│  ├────────────────────────────────┤ │
│  │ 🔥 今天                         │ │
│  │ 💬 初始化项目架构              │ │
│  │ 💬 技术选型讨论                │ │
│  ├────────────────────────────────┤ │
│  │ 📅 昨天                         │ │
│  │ 💬 React 组件设计              │ │
│  │ 💬 API 接口设计                │ │
│  ├────────────────────────────────┤ │
│  │ 📅 更早                         │ │
│  │ 💬 Bug 修复 #42                │ │
│  └────────────────────────────────┘ │
└─────────────────────────────────────┘
```

---

### 3. 📝 消息历史 (Message History)
**数据路径**: `/storage/message/`

- **消息列表**: 时间线展示
- **消息类型**:
  - 💬 用户输入
  - 🤖 AI 回复
  - 🛠️ 工具调用
  - 📄 代码块
  - 🖼️ 图片/文件
- **消息元数据**:
  - 时间戳
  - Token 消耗
  - 模型信息
  - 执行时长

**UI 参考**:
```
┌─────────────────────────────────────┐
│  💬 会话标题 - React 项目初始化      │
│                                     │
│  👤 用户 10:23                      │
│  帮我创建一个 React 项目            │
│                                     │
│  🤖 Claude 10:23                    │
│  好的，我来帮你创建 React 项目。    │
│                                     │
│  🛠️ 工具调用: bash_execute          │
│  $ npx create-react-app my-app      │
│  ✅ 执行成功 (2.3s)                 │
│                                     │
│  🤖 Claude 10:25                    │
│  项目创建完成！                     │
│  ```bash                            │
│  cd my-app && npm start             │
│  ```                                │
│                                     │
│  ┌────────────────────────────────┐ │
│  │ 💭 输入消息...                 │ │
│  └────────────────────────────────┘ │
└─────────────────────────────────────┘
```

---

### 4. 🛠️ 工具执行 (Tool Execution)
**数据路径**: `/storage/part/` (678个部件)

- **工具类型**:
  - 📁 文件操作 (read, write, edit)
  - 🔍 搜索 (grep, glob)
  - ⚡ 命令执行 (bash)
  - 🌐 Web 获取
  - 🧩 Skill 调用
- **执行状态**:
  - ⏳ 执行中
  - ✅ 成功
  - ❌ 失败
  - 🔄 重试
- **执行详情**:
  - 命令/参数
  - 输出结果
  - 执行时长
  - 错误信息

**UI 参考**:
```
┌─────────────────────────────────────┐
│  🛠️ 工具执行                         │
│  ┌────────────────────────────────┐ │
│  │ 📄 read: src/App.tsx          │ │
│  │ ✅ 成功 (12ms)                │ │
│  │ 大小: 1.2KB                   │ │
│  └────────────────────────────────┘ │
│  ┌────────────────────────────────┐ │
│  │ ✏️  edit: src/App.tsx         │ │
│  │ ✅ 成功 (23ms)                │ │
│  │ 变更: +5 -2 lines             │ │
│  └────────────────────────────────┘ │
│  ┌────────────────────────────────┐ │
│  │ ⚡ bash: npm install           │ │
│  │ ✅ 成功 (45s)                 │ │
│  │ 输出: 284 packages installed  │ │
│  └────────────────────────────────┘ │
└─────────────────────────────────────┘
```

---

### 5. 📊 会话差异/变更追踪 (Session Diff)
**数据路径**: `/storage/session_diff/`

- **代码变更**: 会话中的所有文件修改
- **Diff 视图**:
  - 文件列表（按修改时间排序）
  - Side-by-side diff
  - 内联 diff
  - 语法高亮
- **变更统计**:
  - 新增行数
  - 删除行数
  - 修改文件数
- **操作**:
  - 对比任意两个会话状态
  - 导出变更
  - 回滚到某个状态

**UI 参考**:
```
┌─────────────────────────────────────┐
│  📊 会话变更                         │
│  ┌────────────────────────────────┐ │
│  │ 📄 src/App.tsx                 │ │
│  │    +25 -5 lines                │ │
│  │ 📄 src/components/Button.tsx   │ │
│  │    +42 -0 lines (新增)         │ │
│  │ 📄 package.json                │ │
│  │    +3 -1 lines                 │ │
│  └────────────────────────────────┘ │
│  共 3 个文件，+70 -6 行              │
└─────────────────────────────────────┘
```

---

### 6. ✅ 待办事项 (Todo)
**数据路径**: `/storage/todo/`

- **任务列表**:
  - 待办 (TODO)
  - 进行中 (In Progress)
  - 已完成 (Done)
- **任务属性**:
  - 标题
  - 描述
  - 优先级 (P0/P1/P2)
  - 截止时间
  - 标签
- **视图**:
  - 列表视图
  - 看板视图 (Kanban)
  - 日历视图

**UI 参考**:
```
┌─────────────────────────────────────┐
│  ✅ 待办事项                         │
│  ┌──────────┬──────────┬──────────┐ │
│  │ 待办     │ 进行中   │ 已完成   │ │
│  ├──────────┼──────────┼──────────┤ │
│  │ 🔴 P0    │ 🟡 P1    │ ✅ P0    │ │
│  │ 创建项目 │ 设计 UI  │ 初始化   │ │
│  │ 🔴 P0    │          │          │ │
│  │ API开发  │          │          │ │
│  │ 🟡 P1    │          │          │ │
│  │ 搜索功能 │          │          │ │
│  └──────────┴──────────┴──────────┘ │
└─────────────────────────────────────┘
```

---

### 7. 🎯 Skill 管理
**数据路径**: `/Users/yyp/.opencode/skills/`

- **Skill 列表**:
  - 已安装 Skill
  - 可用 Skill (市场)
  - 最近使用
- **Skill 详情**:
  - 名称/描述
  - 版本
  - 使用次数
  - 文档说明
- **操作**:
  - 安装/卸载
  - 启用/禁用
  - 更新
  - 查看源码

**UI 参考**:
```
┌─────────────────────────────────────┐
│  🎯 Skills                           │
│  ┌────────────────────────────────┐ │
│  │ 已安装 (3)                     │ │
│  ├────────────────────────────────┤ │
│  │ 🎨 project-manager            │ │
│  │    v1.0.0 | 已启用 | 12次使用 │ │
│  │ 🧪 testing                    │ │
│  │    v2.1.0 | 已启用 | 5次使用  │ │
│  │ 📊 analytics                  │ │
│  │    v1.2.0 | 已禁用            │ │
│  ├────────────────────────────────┤ │
│  │ 发现更多...                    │ │
│  └────────────────────────────────┘ │
└─────────────────────────────────────┘
```

---

### 8. 📈 统计与洞察

- **使用统计**:
  - 总会话数
  - 总消息数
  - Token 消耗
  - 工具调用次数
- **时间分布**:
  - 每日活跃度
  - 高峰时段
  - 使用趋势
- **Top 技能**: 使用频率最高的 Skill
- **项目统计**: 每个项目的活跃度

**UI 参考**:
```
┌─────────────────────────────────────┐
│  📈 使用统计                         │
│  ┌────────────────────────────────┐ │
│  │ 总会话: 128                    │ │
│  │ 总消息: 1,024                  │ │
│  │ Token: 2.5M                    │ │
│  │ 工具调用: 356                  │ │
│  └────────────────────────────────┘ │
│  📊 最近 7 天活动趋势                 │
│  ▓▓▓▓▓▓▓▓▓▓░░░░▓▓▓▓▓▓▓▓            │
└─────────────────────────────────────┘
```

---

### 9. ⚙️ 设置 (Settings)
**数据路径**: `/Users/yyp/Library/Application Support/ai.opencode.desktop/`

- **通用设置**:
  - 主题 (Light/Dark)
  - 语言
  - 字体大小
  - 通知设置
- **模型设置**:
  - 默认模型
  - API Keys
  - 温度/参数
- **界面设置**:
  - 窗口状态记忆
  - 快捷键
  - 布局偏好
- **数据管理**:
  - 导出/导入数据
  - 清除历史
  - 备份恢复

---

### 10. 🔍 全局搜索

- **搜索范围**:
  - 所有会话消息
  - 文件名
  - 代码片段
  - Skill 名称
- **搜索过滤器**:
  - 时间范围
  - 项目
  - 会话类型
  - 文件类型

---

## 为 Claude Web UI 提取的核心功能

基于 OpenCode 桌面版，建议 Claude Web UI 优先实现：

### 🔴 P0 (必须有)

1. **Dashboard**
   - 最近会话列表
   - 快速开始新会话
   - 统计概览

2. **会话管理**
   - 会话列表（按时间分组）
   - 会话详情（消息历史）
   - 新建/重命名/删除

3. **历史记录查看**
   - 消息时间线
   - 工具执行记录
   - 代码变更追踪

4. **Skill 管理**
   - Skill 列表
   - Skill 详情查看
   - 启用/禁用

### 🟡 P1 (应该有)

5. **全局搜索**
   - 搜索历史消息
   - 搜索文件/代码

6. **待办事项**
   - 简单任务列表
   - 状态管理

7. **项目切换**
   - 工作区管理
   - 最近项目

### 🟢 P2 (可以有)

8. **统计洞察**
   - 使用数据分析
   - 趋势图表

9. **导出功能**
   - 导出会话
   - 导出变更

10. **设置**
    - 主题切换
    - 快捷键

---

## UI 布局参考

### 桌面端 (三栏布局)
```
┌─────────────────────────────────────────────────────────────┐
│  🎯 Header: 搜索 | 设置 | 用户信息                          │
├──────────┬──────────────────────────┬──────────────────────┤
│          │                          │                      │
│  📁      │      💬                  │      🛠️              │
│  项目    │      聊天                │      工具/技能       │
│  导航    │      区域                │      面板            │
│          │                          │                      │
│  ────────┼──────────────────────────┤                      │
│          │                          │                      │
│  💬      │                          │                      │
│  会话    │                          │                      │
│  列表    │                          │                      │
│          │                          │                      │
├──────────┴──────────────────────────┴──────────────────────┤
│  💭 输入框                                                   │
└─────────────────────────────────────────────────────────────┘
```

### 移动端 (单栏 + Tab)
```
┌─────────────────────────────────────┐
│  🎯 Header                          │
├─────────────────────────────────────┤
│                                     │
│      💬 聊天区域                     │
│                                     │
├─────────────────────────────────────┤
│  💭 输入框                           │
├─────────────────────────────────────┤
│  📁  💬  🛠️  ⚙️                     │
│  项目  会话  工具  设置               │
└─────────────────────────────────────┘
```

---

## 数据结构参考

### 会话 (Session)
```typescript
interface Session {
  id: string;
  title: string;
  projectId: string;
  createdAt: DateTime;
  updatedAt: DateTime;
  messageCount: number;
  status: 'active' | 'archived';
}
```

### 消息 (Message)
```typescript
interface Message {
  id: string;
  sessionId: string;
  role: 'user' | 'assistant' | 'system';
  content: string;
  timestamp: DateTime;
  tools?: ToolCall[];
  metadata?: {
    model?: string;
    tokens?: number;
    duration?: number;
  };
}
```

### 工具调用 (ToolCall)
```typescript
interface ToolCall {
  id: string;
  tool: string;
  params: Record<string, any>;
  status: 'pending' | 'running' | 'success' | 'error';
  result?: any;
  error?: string;
  startedAt: DateTime;
  completedAt?: DateTime;
}
```

### Skill
```typescript
interface Skill {
  id: string;
  name: string;
  description: string;
  version: string;
  installed: boolean;
  enabled: boolean;
  usageCount: number;
  content: string;  // SKILL.md 内容
}
```

---

**文档版本**: 1.0  
**参考来源**: OpenCode 桌面版数据结构  
**最后更新**: 2025-02-10  
